<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS Movie Library - Home</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: Arial, sans-serif; background: #f4f4f4; min-height: 100vh; padding: 2rem; }
        .container { max-width: 600px; margin: 0 auto; background: white; padding: 2rem; border-radius: 10px; box-shadow: 0 5px 15px rgba(0,0,0,0.1); text-align: center; }
        h1 { color: #333; margin-bottom: 1rem; font-size: 2rem; }
        h2 { color: #666; margin-bottom: 2rem; }
        .routes { list-style: none; }
        .route-item { margin: 1rem 0; }
        .route-link { display: block; padding: 1rem; background: #007bff; color: white; text-decoration: none; border-radius: 5px; transition: background 0.3s; }
        .route-link:hover { background: #0056b3; }
        @media (max-width: 768px) {
            body { padding: 1rem; }
            .container { padding: 1.5rem; }
            h1 { font-size: 1.5rem; }
            .route-link { padding: 0.8rem; font-size: 0.9rem; }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🎬 Movie Library</h1>
        <h2>Available Routes</h2>
        <ul class="routes">
            <li class="route-item"><a href="/movie" class="route-link">Single Movie</a></li>
            <li class="route-item"><a href="/movie-with-image" class="route-link">All Movies</a></li>
        </ul>
    </div>
</body>
</html>